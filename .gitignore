.idea

*.yaml
!.pre-commit-config.yaml
!example-config.yaml

*.json
*.db*
*.log
*.bak

/mautrix-whatsapp
/start

# The production inventory (real host, user and port) stays out of this
# public fork; CI writes it from secrets, see .github/workflows/release.yml.
/deploy/ansible/inventory.ini
